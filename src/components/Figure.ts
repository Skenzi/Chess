class Figure {
  x: number;

  y: number;

  type: string;

  color: string;

  constructor(x: number, y: number, type: string, color: string) {
    this.x = x;
    this.y = y;
    this.type = type;
    this.color = color;
  }
}

export default Figure;
